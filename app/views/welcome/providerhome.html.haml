%h1 PROVIDER DASHBOARD
%p Please select the provider you wish to review

%table.table.table-hover
	%thead
		%tr
			%th 
			%th View Provider
			%th Provider Name
			%th Provider Specialty
			%th{:colspan => "3"}
	%tbody
		- current_user.providers.each do |provider|
			%tr
				%td= link_to (image_tag provider.image.url(:thumb)), provider
				%td= link_to provider.npi, provider
				%td= provider.name
				%td= provider.specialty.specialty

= link_to "Add Provider", new_provider_path, class: "btn btn-default" 

